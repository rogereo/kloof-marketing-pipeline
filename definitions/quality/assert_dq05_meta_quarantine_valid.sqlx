config { type: "assertion", schema: "kloof_ops", tags: ["quality", "quality_batch1", "DQ05"] }

SELECT row_fingerprint, raw_line
FROM ${ref("quarantine_meta_ads")}
WHERE SAFE.PARSE_JSON(raw_line) IS NOT NULL
