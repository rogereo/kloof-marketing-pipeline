config { type: "assertion", schema: "kloof_ops", tags: ["quality", "quality_batch1", "DQ01"] }

SELECT order_id, COUNT(*) AS row_count
FROM ${ref("stg_crm_orders")}
GROUP BY order_id
HAVING COUNT(*) > 1
